{
  "manifest_version": 2,

  "name": "Buildkite Build Autorun",
  "description": "This extension allows the user to automatically run and log the failing tests in an open buildkite pipeline",
  "version": "1.0",

  "browser_action": {
    "default_icon": "icon.png"
  },
  "background": {
    "scripts": ["jquery-3.2.1.slim.min.js", "background.js"]
  },
  "permissions": [
    "activeTab"
  ]
}
